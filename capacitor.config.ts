import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.krlcommuter.smartalarm',
  appName: 'KRL Commuter Smart Alarm',
  webDir: 'dist'
};

export default config;
